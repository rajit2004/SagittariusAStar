/// Shared design dimension tokens used across the app.
/// Replaces hardcoded padding, spacing, radius, and icon size values.
class RhythmaDimens {
  RhythmaDimens._();

  // ── Page-level padding ──────────────────────────────────────────
  static const double pageHorizontal = 20;
  static const double pageBottomOverflow = 100;

  // ── Section spacing ────────────────────────────────────────────
  static const double gapSmall = 8;
  static const double gapMedium = 10;
  static const double gapDefault = 14;
  static const double gapLarge = 16;
  static const double gapXl = 24;
  static const double gapXxl = 32;

  // ── Card border radius ─────────────────────────────────────────
  static const double radiusSmall = 10;
  static const double radiusMedium = 16;
  static const double radiusLarge = 20;
  static const double radiusSheet = 24;
  static const double radiusPill = 28;

  // ── Icon sizes ─────────────────────────────────────────────────
  static const double iconSmall = 14;
  static const double iconMedium = 17;
  static const double iconLarge = 18;
  static const double iconXl = 20;

  // ── Tinted icon container ──────────────────────────────────────
  static const double tintedIconSize = 34;
  static const double tintedIconRadius = 10;

  // ── Header icon (language, shield, etc.) ───────────────────────
  static const double headerIconSize = 38;

  // ── Stat ring sizes ────────────────────────────────────────────
  static const double cycleRingSize = 88;
  static const double scoreRingSize = 96;

  // ── Avatar ─────────────────────────────────────────────────────
  static const double avatarRadius = 48;

  // ── Text field ─────────────────────────────────────────────────
  static const double textFormFieldHeight = 50;

  // ── Calendar ───────────────────────────────────────────────────
  static const double calendarCellHeight = 46;
  static const double calendarDaySize = 34;
  static const double calendarPageViewHeight = 330;

  // ── Learn card ─────────────────────────────────────────────────
  static const double learnCardWidth = 152;
  static const double learnCardHeight = 128;

  // ── Chat ───────────────────────────────────────────────────────
  static const double chatAvatarSize = 28;
  static const double chatDotSize = 7;
}
